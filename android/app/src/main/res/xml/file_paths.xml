<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cho phép chia sẻ file từ thư mục external files của app -->
    <external-files-path
        name="cabinet_images"
        path="Pictures/" />
    <!-- Fallback: cache nội bộ -->
    <cache-path
        name="cabinet_cache"
        path="." />
</paths>
